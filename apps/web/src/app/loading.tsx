export default function Loading() {
  return (
    <div className="page-stack">
      <section className="hero-panel">
        <p className="page-header__eyebrow">Loading</p>
        <h1>正在准备页面数据...</h1>
        <p>当前是 mock-first 原型，稍候将展示最新快照、榜单和 AI 任务状态。</p>
      </section>
    </div>
  );
}
