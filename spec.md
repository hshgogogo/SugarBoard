# SSBoard v1 产品规格说明

Status: ready_for_architecture
Owner: product-analyst
Last Updated: 2026-04-01

## 1. 产品目标

SSBoard v1 的目标是交付一个**面向内部团队的桌面端 Web 原型**，让内容策划、宣发、行业分析和管理层能够在同一个系统内完成以下闭环：

1. 查看四类核心榜单：艺人、角色、剧集、电影。
2. 通过统一筛选条件查看榜单和趋势变化。
3. 从榜单下钻到实体详情页，理解作品 / 艺人 / 角色的上下文。
4. 在 AI 工作台中用自然语言发起**只读**数据分析，并获得可追溯的图表和中文摘要。

### 1.1 v1 成功标准

1. 用户可在 3 分钟内完成“首页查看 -> 榜单筛选 -> 实体下钻 -> AI 分析”的完整流程。
2. 所有页面展示的数据都能明确看到来源范围与更新时间。
3. AI 仅访问白名单分析视图，且对不安全请求进行拒绝或限缩。
4. 产品足够稳定，供内部演示、评审和下一阶段数据接入使用。

### 1.2 产品边界定义

SSBoard v1 是**行业数据分析原型**，不是：

- 通用 BI 平台
- 面向外部客户的 SaaS 产品
- 完整数据治理 / 数据运营后台
- 任意脚本执行平台

## 2. 目标用户与关键任务

| 角色 | 关键任务 | v1 需要解决的问题 |
| --- | --- | --- |
| 行业分析师 | 快速比较榜单、趋势、平台差异 | 减少跨平台搬运数据和手工出图 |
| 宣发同学 | 观察竞品、艺人、角色热度变化 | 快速看涨跌、题材、来源差异 |
| 内容策划 | 判断选题、阵容、角色表现 | 从榜单快速下钻到作品 / 艺人 / 角色详情 |
| 管理层 | 快速看当前大盘和重点变化 | 在首页获得概览，并通过 AI 得到摘要 |

## 3. v1 范围收敛结论

### 3.1 v1 必做页面

v1 仅交付 4 个一级页面能力：

1. **首页总览**
2. **榜单页**（同一页面范式，按榜单类型切换）
3. **实体详情页**（同一页面范式，覆盖作品 / 艺人 / 角色）
4. **AI 查询工作台**

### 3.2 v1 范围收敛原则

为保证这是一个可落地的内部原型，做以下收敛：

1. **只做桌面端**，不要求移动端适配。
2. **只做内部使用场景**，不做复杂权限、多租户、外部分享。
3. **只展示预计算榜单与分析视图**，不在产品内编辑榜单公式。
4. **AI 只做读数据分析**，不做任意 Python 脚本执行。
5. **AI 结果留在工作台内展示**，不单独做结果页。
6. **详情页用统一模板承载差异化模块**，不拆成多套完全不同页面。
7. **关系图简化为关系列表 / 卡片**，不做复杂交互式知识图谱。
8. **数据导入、来源管理、公式配置**均不做前台可配置 UI。

## 4. 页面与功能范围

### 4.1 首页总览

首页用于回答“今天内部最值得先看的内容是什么”。

#### 必须包含的模块

1. 顶部筛选区
   - 时间
   - 来源
   - 平台
   - 题材
2. KPI 卡片区
   - 在榜作品数
   - 上新作品数
   - 热度涨幅冠军
   - 数据覆盖率
3. 四类榜单卡片区
   - 艺人热榜 Top 5
   - 角色热榜 Top 5
   - 剧集热榜 Top 5
   - 电影热榜 Top 5
4. 分析图区
   - 趋势分析图
   - 来源对比或题材 / 平台分布图
5. AI 快捷入口区
   - 自然语言输入框
   - 推荐问题
   - 最近分析任务（最近 5 条即可）

#### v1 行为要求

1. 默认进入**最新可用快照日期**。
2. 首页的筛选条件应作用于 KPI、榜单卡片和分析图。
3. 点击任一榜单卡片，可跳转到对应榜单页，并尽量保留当前筛选条件。
4. 首页必须始终显示当前所用数据的**来源范围**与**最后更新时间**。

#### v1 明确不做

- 自定义拖拽式仪表盘
- 首页直接导出图片 / PDF
- 全局模糊搜索作为验收项

### 4.2 榜单页

榜单页用于回答“某类对象在当前口径下谁更强、最近怎么变”。

#### 支持的榜单类型

1. 艺人榜
2. 角色榜
3. 剧集榜
4. 电影榜

#### 必须包含的模块

1. 左侧筛选栏
   - 榜单日期
   - 来源
   - 平台
   - 题材
   - 排序方式
2. 中间榜单主表
   - 排名
   - 名称
   - 分值
   - 涨跌值 / 变化趋势
   - 标签
   - 来源提示
   - 快照日期
3. 右侧辅助分析区
   - 当前筛选结果的趋势对比图
   - 来源拆解 / 组成分析
   - 相关标签汇总

#### v1 行为要求

1. 榜单页采用**同一页面结构 + 榜单类型切换**的方式实现，而不是 4 套独立产品逻辑。
2. 默认按分值降序展示。
3. 单页可先支持 Top 100；如实现成本高，至少保障 Top 50 可浏览。
4. 点击榜单行进入对应实体详情页。
5. 若当前筛选组合下无数据，需提供明确空状态与回退建议。

#### v1 明确不做

- 用户自定义榜单公式
- 用户自定义表格列管理
- 榜单结果导出 Excel / CSV 作为验收项

### 4.3 实体详情页

实体详情页用于回答“这个对象为什么在榜、近期走势如何、和谁 / 什么作品有关”。

#### 支持的实体类型

1. **作品**：覆盖电影与剧集，使用同一详情页模板，通过 subtype 区分
2. **艺人**
3. **角色**

#### 必须包含的模块

1. 基础信息卡
   - 名称
   - 头像 / 海报
   - 标签
   - 平台 / 题材 / 作品类型（按实体类型显示）
   - 最后更新时间
2. 指标与趋势区
   - 当前分值 / 排名
   - 最近 30 天趋势图（默认）
   - 近期榜单轨迹
3. 关联信息区
   - 作品详情：演员阵容、角色列表、平台信息
   - 艺人详情：代表作品、近期合作对象
   - 角色详情：所属作品、饰演者、相关热度表现
4. 事件时间线
   - 定档 / 上映 / 开播 / 公开事件等
5. 来源与口径区
   - 该详情页所依赖的数据来源范围
   - 快照日期 / 更新时间
   - 口径说明入口

#### v1 行为要求

1. 详情页必须可从榜单页直接进入。
2. 详情页的核心趋势必须依赖已入库的日维度快照，不要求实时计算。
3. 若部分实体缺少某些关系数据，页面仍应可打开，并对缺失模块显示空状态说明。
4. v1 关系模块以**列表 / 卡片 / 简单关系块**为主，不要求交互式关系图。

### 4.4 AI 查询工作台

AI 工作台用于回答“我能不能直接用自然语言问出一个图表和结论”。

#### v1 支持的问题类型

1. **榜单查询**
   - 例：最近一周电影热榜前十是谁？
2. **趋势对比**
   - 例：某两部剧过去 30 天热度走势对比
3. **分布对比**
   - 例：当前剧集热榜按平台分布如何
4. **变化总结**
   - 例：最近 7 天艺人榜波动最大的对象是谁，变化如何

#### 必须包含的模块

1. 左侧输入与任务区
   - 问题输入框
   - 推荐问题
   - 任务列表 / 最近记录
2. 右侧执行与结果区
   - 问题理解 / 执行逻辑说明
   - SQL 解释或等价查询说明
   - 风险等级
   - 数据范围说明
   - 结果图表
   - 中文摘要

#### v1 行为要求

1. AI 仅面向白名单分析视图发起**只读查询**。
2. 对于可安全执行的问题，必须返回：
   - 查询解释
   - 风险等级
   - 数据范围
   - 图表结果
   - 中文摘要
3. 对于不支持或不安全的问题，必须返回：
   - 明确拒绝 / 限缩原因
   - 建议用户如何缩小问题范围
4. 工作台保留最近任务状态，至少支持：
   - succeeded
   - failed
   - blocked / needs_refine
5. v1 不要求任意脚本沙箱执行；若问题超出白名单分析能力，应安全拒绝，而不是尝试执行。

#### v1 明确不做

- 任意 Python / notebook 执行
- 对外分享 AI 结果
- 自动生成完整汇报材料
- 独立 AI 结果详情页

## 5. 数据边界与来源追溯要求

### 5.1 业务数据边界

1. 领域范围限定在**中国影视行业分析**。
2. v1 核心实体仅覆盖：
   - works（电影 / 剧集）
   - persons（以艺人为主）
   - characters
   - platforms
   - sources
3. 榜单与趋势以**日维度快照**为主。
4. 首页、榜单页、详情页、AI 工作台全部是**只读消费端**，不允许修改原始数据。
5. v1 数据主要来自：
   - 官方 / 开放 API
   - 公开公示数据
   - 已授权商业数据导出
   - 人工导入文件
6. **未授权商业平台自动抓取**不能作为 v1 主方案。

### 5.2 每条可展示数据必须可追溯的业务信息

架构设计必须保证，任意榜单记录、图表点位或 AI 结果都能追溯到以下业务语义：

| 追溯字段 | 说明 |
| --- | --- |
| entity_id / entity_type | 这条数据对应的对象是谁 |
| metric_or_ranking_type | 这条数据属于什么指标或榜单 |
| snapshot_date | 数据代表的业务日期 |
| source_id / source_name | 数据来自哪个来源 |
| source_type | 官方 API / 公开公示 / 授权导出 / 手工导入 |
| authorization_status | 授权状态 |
| ingested_at / updated_at | 实际进入系统或最后更新时间 |
| caliber_note | 口径说明或映射说明 |

> 说明：具体物理字段名由后续架构定义，但这些业务含义不能缺失。

### 5.3 前台页面的追溯展示义务

1. **首页**必须显示当前筛选结果所依赖的数据来源范围与最后更新时间。
2. **榜单页**的每条记录至少要能一跳查看来源提示与快照日期；可以是行内字段，也可以是轻量提示层。
3. **详情页**的趋势图和关键模块必须展示其来源范围与更新时间。
4. **AI 结果**必须显示：
   - 使用的数据范围
   - 使用的来源范围
   - 风险等级
   - 执行时间 / 状态
5. 如果同一结果混合了多个来源，必须显式标记“多来源聚合”，不能让用户误以为来自单一来源。

## 6. AI 风险分级与安全约束

### 6.1 白名单能力边界

v1 AI 只允许访问面向分析的白名单视图，不能直接读写底层业务表。

#### 允许的能力

1. 只读 `SELECT`
2. 有界日期范围查询
3. 有界结果集返回
4. 基于查询结果自动选择图表
5. 生成简短中文分析摘要

#### 禁止的能力

1. `INSERT / UPDATE / DELETE / ALTER / DROP`
2. 访问非白名单原始表
3. 任意 Python / Shell / 外部网络抓取
4. 上传或下载外部文件作为查询输入输出
5. 绕过来源授权状态访问数据
6. 生成不可解释的“黑盒”结论且不附带数据范围说明

### 6.2 风险等级规则

#### Green（允许直接执行）

满足以下条件的问题可直接执行：

1. 可转成单次只读查询
2. 仅访问白名单分析视图
3. 时间范围明确且有界
4. 返回结果可控（建议上限 200 行以内）
5. 不涉及写操作、外部调用、脚本执行

#### Yellow（不直接执行，要求限缩）

出现以下情况时，不应直接执行：

1. 问题范围过大或时间跨度过宽
2. 条件歧义明显，可能造成误导性结论
3. 结果规模过大，不适合直接在工作台展示

v1 对 Yellow 的最小要求是：

- 不自动执行
- 返回限缩建议
- 记录任务状态为 `blocked` 或 `needs_refine`

#### Red（明确拒绝）

出现以下情况时必须拒绝：

1. 非只读意图
2. 访问非白名单数据
3. 要求执行脚本 / 下载导出 / 外部抓取
4. 涉及高风险来源或越权访问

### 6.3 AI 审计最小要求

每次 AI 请求至少要记录以下信息，供后续架构与 QA 使用：

1. 原始问题
2. 解析后的意图类型
3. 风险等级
4. 生成的 SQL 或等价查询描述
5. 执行状态
6. 返回行数
7. 执行耗时
8. 时间戳

## 7. 原型数据准备最低要求

为保证页面与 AI 能实际联调，v1 需要至少具备以下演示数据能力：

1. 四类榜单在**最新快照**下均有可展示数据。
2. 每类榜单至少有 20 条记录可用于页面展示；如时间不足，最低不能少于 10 条。
3. 首页和详情页趋势至少具备最近 30 天的演示数据。
4. 作品、艺人、角色三类详情页都必须有可打开的样例实体。
5. 至少覆盖 2 类来源类型（如开放源 + 授权导入），以验证来源追溯 UI。

## 8. 用户故事

| ID | 优先级 | 用户故事 |
| --- | --- | --- |
| US-01 | P0 | 作为行业分析师，我希望一进首页就看到最新快照下的四类榜单概览和核心 KPI，这样我能快速判断今天先看什么。 |
| US-02 | P0 | 作为宣发同学，我希望能在榜单页按日期、来源、平台、题材筛选并看涨跌，这样我能快速锁定目标对象。 |
| US-03 | P0 | 作为内容策划，我希望从榜单直接下钻到作品 / 艺人 / 角色详情页，这样我能理解对象背后的上下文。 |
| US-04 | P0 | 作为管理层，我希望所有页面都能明确告诉我数据来源和更新时间，这样我在内部沟通时不会误用数据。 |
| US-05 | P0 | 作为分析师，我希望在 AI 工作台输入自然语言后，拿到可解释的图表和中文摘要，这样我不需要先写 SQL。 |
| US-06 | P0 | 作为内部使用者，我希望系统在 AI 请求不安全或超范围时明确拒绝，而不是返回看似合理但不可追溯的结果。 |
| US-07 | P1 | 作为用户，我希望看到最近 AI 任务和状态，这样我能知道刚才的问题是成功、失败还是需要重试。 |
| US-08 | P1 | 作为用户，我希望在无数据、数据不完整、任务失败时看到可读的空状态或错误说明，这样我知道下一步怎么做。 |

## 9. 验收标准

### 9.1 首页总览验收

#### AC-01 首页默认态

**Given** 系统存在最新可用快照
**When** 用户进入首页
**Then** 页面应默认加载最新快照，并展示：

1. 4 个 KPI 卡片
2. 4 个榜单卡片
3. 至少 1 个趋势 / 分布分析模块
4. AI 快捷入口与最近任务列表
5. 当前来源范围与最后更新时间

#### AC-02 首页筛选联动

**Given** 用户在首页修改时间、来源、平台或题材筛选
**When** 筛选生效
**Then** KPI、榜单卡片和分析图必须同步更新或显示明确加载状态。

#### AC-03 首页跳转

**Given** 用户点击某个榜单卡片
**When** 进入榜单页
**Then** 页面必须进入对应榜单类型，且尽量保留首页已选择的公共筛选条件。

### 9.2 榜单页验收

#### AC-04 榜单切换与筛选

**Given** 用户进入任一榜单页
**When** 切换榜单类型或修改筛选条件
**Then** 页面必须刷新榜单主表和右侧辅助分析区。

#### AC-05 榜单主表最小信息

榜单主表每一行至少必须可读到或一跳查看到：

1. 排名
2. 名称
3. 分值
4. 涨跌信息
5. 快照日期
6. 来源提示

#### AC-06 榜单下钻

**Given** 用户点击榜单中的有效行
**When** 系统完成跳转
**Then** 必须打开对应实体详情页，而不是停留在榜单页内弹层。

#### AC-07 榜单空状态

**Given** 当前筛选条件下无数据
**When** 页面渲染完成
**Then** 必须展示空状态说明，并提示用户调整日期、来源或筛选范围。

### 9.3 实体详情页验收

#### AC-08 详情页基础模块

作品 / 艺人 / 角色详情页都必须包含：

1. 基础信息区
2. 指标或排名信息
3. 趋势图
4. 关联信息区
5. 来源与更新时间信息

#### AC-09 详情页差异化内容

1. 作品详情页需展示演员阵容或角色相关信息。
2. 艺人详情页需展示代表作品或近期合作对象。
3. 角色详情页需展示所属作品与饰演者。

#### AC-10 缺失数据容错

**Given** 某个实体缺少部分关系数据或事件时间线
**When** 用户进入详情页
**Then** 页面仍应正常打开，并在缺失模块显示可读说明，不得整页报错。

### 9.4 AI 工作台验收

#### AC-11 支持问题的成功路径

**Given** 用户输入一个 Green 范围内的问题
**When** 系统执行成功
**Then** 结果区必须同时展示：

1. 查询解释或 SQL 说明
2. 风险等级
3. 数据范围说明
4. 图表结果
5. 中文摘要
6. 执行状态

#### AC-12 超范围问题的安全处理

**Given** 用户输入 Yellow 或 Red 范围的问题
**When** 系统判断不应直接执行
**Then** 系统必须：

1. 不执行危险查询
2. 返回明确原因
3. 给出缩小范围或改写问题的建议
4. 记录任务状态

#### AC-13 最近任务列表

AI 工作台至少应展示最近任务的：

1. 问题摘要
2. 时间
3. 状态（succeeded / failed / blocked / needs_refine）

### 9.5 来源追溯与数据边界验收

#### AC-14 来源追溯可见性

首页、榜单页、详情页、AI 工作台 4 类页面都必须能让用户看到当前结果依赖的来源范围和更新时间。

#### AC-15 多来源聚合提示

**Given** 某个结果来自多个来源
**When** 页面展示该结果
**Then** 必须显式提示“多来源聚合”或等价文案。

#### AC-16 只读边界

AI 相关实现不得允许用户通过自然语言触发任何写操作、外部抓取或任意脚本执行。

### 9.6 非功能验收

#### AC-17 性能目标

在内部原型样例数据规模下：

1. 首页和榜单页首屏目标应接近 1.5 秒内返回核心内容
2. Green 类 AI 查询目标应在 10 秒内返回结果
3. 无法快速完成的请求必须进入失败 / 阻塞态，而不是长时间无反馈

#### AC-18 终端范围

v1 只要求桌面端可用，建议最小宽度按 1280px 设计；移动端不作为验收项。

#### AC-19 语言与可读性

v1 页面文案与 AI 摘要以中文为主，错误信息应为人可读文本，不接受纯技术报错直出。

## 10. 明确延期到 v2 之后的事项

以下内容不属于当前 v1 验收范围：

1. 多租户与复杂权限体系
2. 对外分享链接 / 外部客户访问
3. 自助数据导入后台
4. 来源管理后台
5. 榜单公式可视化配置
6. 全局实体搜索
7. 表格 / 图表导出
8. 复杂交互式关系图谱
9. 任意 Python 沙箱分析
10. AI 自动生成完整汇报材料
11. 移动端适配

## 11. 交接给架构阶段的默认业务假设

若后续没有新指令覆盖，架构阶段应默认采用以下业务假设：

1. 榜单分值由离线或导入流程预先计算，前台只负责消费与展示。
2. 电影与剧集共用作品详情页模板，通过实体 subtype 区分。
3. AI 工作台先完整实现 Green 路径与 Red 拒绝路径，Yellow 以“限缩建议 + 阻塞状态”满足 v1。
4. AI 结果不拆独立页面，保留在同一工作台内。
5. 来源追溯是全站硬约束，不是某个页面的可选增强项。
