# Frontend

当前前端代码统一放在 `frontend/web`。

- 技术栈：Next.js + React + TypeScript
- 运行入口：`frontend/web`
- 本地端口：建议 `3001`

启动命令：

```bash
cd frontend/web
npm install
npm run dev -- --hostname 127.0.0.1 --port 3001
```
